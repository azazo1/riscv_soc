# TODO

## 当前目标

- [ ] 明确第一阶段目标: 在 DE1-SoC 上跑通一个可执行 Rust 裸机程序的 RV32I CPU.
- [ ] 明确第一阶段不做的内容: Linux, MMU, 复杂缓存一致性, HPS 协同.
- [ ] 整理开发顺序: 先本地仿真闭环, 再接板级外设, 最后上板验证.

## 总体架构设计

- [ ] 画出第一版 SoC 框图: CPU, ROM, RAM, UART, LED, HEX, timer.
- [ ] 确定内存映射, 包括:
- [ ] reset vector 地址.
- [ ] boot ROM 地址范围.
- [ ] on-chip RAM 地址范围.
- [ ] MMIO 地址范围.
- [ ] 约定总线接口形式, 先用简单同步接口, 不要一开始引入复杂总线协议.
- [ ] 写一份 `docs/arch.md`, 说明 CPU 边界和 SoC 边界.

## 本地仿真基础设施

- [ ] 建立 `build/` 目录下的统一构建输出约定.
- [ ] 扩展 `justfile`, 提供仿真, 测试, 固件转换等常用命令.
- [ ] 搭建 Verilator 顶层 testbench.
- [ ] 为 ROM, RAM, UART, LED 等外设写仿真模型.
- [ ] 让 testbench 支持加载 `mem/hex` 固件镜像.
- [ ] 约定仿真结束条件, 例如 UART 输出特定结束标记或写入退出寄存器.

## RV32I CPU 核心

- [ ] 决定微架构: 单周期, 多周期, 或简单流水.
- [ ] 实现 program counter 和取指.
- [ ] 实现指令译码.
- [ ] 实现寄存器堆, 保证 `x0` 恒为 0.
- [ ] 实现 ALU.
- [ ] 实现立即数生成.
- [ ] 实现分支和跳转.
- [ ] 实现 load/store 和字节使能.
- [ ] 实现写回路径.
- [ ] 实现非法指令和基础 trap 处理策略, 即便第一版只做最小行为.
- [ ] 补充必要 tracing 或调试信号, 方便定位执行过程.

## CPU 级测试

- [ ] 先为关键模块写小型 testbench: ALU, regfile, decoder, branch, load/store.
- [ ] 为 RV32I 指令子集准备最小汇编测试.
- [ ] 覆盖算术, 逻辑, 比较, 分支, 跳转, load/store, `lui`, `auipc`.
- [ ] 增加若干边界测试: `x0`, 符号扩展, 地址对齐, 分支偏移, jump 返回地址.
- [ ] 建立回归命令, 每次改 CPU 后可以一键跑完.

## 简单的图形接口

- [ ] 使用简单的图形接口和图像缓冲区来绘制内容, 不使用软件层面来控制 vga 通信.

## Rust 固件支持

- [ ] 建一个独立的裸机 Rust 固件目录.
- [ ] 使用官方目标 `riscv32i-unknown-none-elf`.
- [ ] 编写最小 `linker.ld`.
- [ ] 编写启动代码: 设置栈, 清零 `.bss`, 搬运 `.data`, 跳转到 `main`.
- [ ] 提供 `panic_handler`.
- [ ] 写一个最小 MMIO 封装, 先支持 UART, LED, HEX.
- [ ] 写至少 2 个样例程序:
- [ ] `hello_uart`.
- [ ] `led_blink` 或内存计算自检程序.
- [ ] 建立 `ELF -> bin/hex/mem` 转换流程, 接到仿真里.

## SoC 联调

- [ ] 把 CPU 和 ROM/RAM/MMIO 连接成可运行的最小 SoC.
- [ ] 先跑通纯计算程序, 再跑 UART 输出程序.
- [ ] 核对 Rust 程序的栈, 全局变量, 只读数据是否都落在预期地址.
- [ ] 观察 UART 输出和寄存器结果, 确认软件硬件语义一致.
- [ ] 增加一个仿真自检程序, 用于 CI 或日常回归.

## 上板准备

- [ ] 新建 Quartus 工程骨架.
- [ ] 整理 DE1-SoC 的 pin 约束来源.
- [ ] 先只接时钟, 复位, LED, HEX, KEY, SW, UART.
- [ ] 确认复位策略和时钟分频策略.
- [ ] 先在板上跑最小 `hello` 或 LED 程序.
- [ ] 记录首次上板问题和修复方式, 写到 `docs/bringup.md`.

## 第二阶段扩展

- [ ] 接入 FPGA 侧 64 MB SDRAM.
- [ ] 增加 timer 和中断.
- [ ] 评估是否需要 `M` 扩展, 以提升 Rust 程序和算法性能.
- [ ] 设计 VGA 输出方案, 明确是调试显示还是图形程序输出.
- [ ] 如果要做 VGA, 先单独完成 VGA 控制器和显存方案, 再接入 CPU.
- [ ] 使用上面提供的简单图形接口来在 rust 层面制作一个可游玩的图形游戏.
- [ ] 看看能不能顺便把 3d ray tracing 的那个代码示例运行起来, 在 vga 上.

## 收尾和维护

- [ ] 检查 `.gitignore`, 补充 Quartus, Verilator, 固件构建产物.
- [ ] 为常用流程补充 `README.md` 和 `justfile` 示例.
- [ ] 保持每个阶段都有一个可运行里程碑, 不要同时推进过多模块.

## 结果展示

- [ ] 拍视频
